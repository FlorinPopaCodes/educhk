{
	"name": "educhk-monorepo",
	"private": true,
	"scripts": {
		"sync": "tsx scripts/sync-swot.ts",
		"build": "pnpm --filter educhk build",
		"test": "pnpm --filter educhk-worker test",
		"deploy": "pnpm --filter educhk-worker run deploy"
	},
	"devDependencies": {
		"tsx": "^4.19.0",
		"typescript": "^5.5.2"
	}
}
